server:
  port: 8080

app:
  storage-root: data
  chunk-size: 500
  cors-allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:*}

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
